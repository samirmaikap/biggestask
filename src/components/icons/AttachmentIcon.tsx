import React from 'react';
import {Text, View} from 'react-native';

type Props = {
  size: number;
  color: string;
  fill: string;
};

export const AttachmentIcon = (props: Props) => {
  return (
    <View>
      <Text>Atta</Text>
    </View>
  );
};
